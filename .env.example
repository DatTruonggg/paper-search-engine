# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG_MODE=true

# Elasticsearch Configuration (using non-conflicting port)
ES_HOST=localhost:9202
ES_INDEX_NAME=papers

# MinIO Configuration (using non-conflicting port)
MINIO_ENDPOINT=localhost:9002
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=false

# BGE Embedding Configuration
BGE_MODEL_NAME=BAAI/bge-large-en-v1.5
BGE_CACHE_DIR=./models

# Search Configuration
MAX_SEARCH_RESULTS=20
DEFAULT_SEARCH_MODE=hybrid
SEARCH_TIMEOUT_SECONDS=30

# Document Processing Configuration
CHUNK_SIZE=512
CHUNK_OVERLAP=100

# Data Paths
MARKDOWN_DATA_PATH=./data/processed/markdown
PDF_DATA_PATH=./data/pdfs
